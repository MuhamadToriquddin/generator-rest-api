import {RunCommand} from "./runcommand.js";

export function SetupSequelize(){
    const tasks = [
        {
            label: "Install Sequelize ORM",
            command: "npm",
            args: ["install", "sequelize"]
        },
        {
            label: "Install Sequelize ORM",
            command: "npm",
            args: ["install", "sequelize"]
        },

    ]
}